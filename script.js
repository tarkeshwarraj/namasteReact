const heading = React.createElement(
  "hi",
  { id: "heading", className: "head" },
  "This is first Rakesh React"
);

const root = ReactDOM.createRoot(document.getElementById("root"));

root.render(heading);
